<?xml version="1.0" encoding="ISO-8859-1"?>
<scripts xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../.gradle/scripts.xsd">

    <sql nomeObjeto="VW_VENC_QUALIF_FORN" tipoObjeto="VIEW" nomeTabela="VW_VENC_QUALIF_FORN" executar="SE_NAO_EXISTIR" ordem="1">
        <oracle>

CREATE OR REPLACE VIEW VW_VENC_QUALIF_FORN AS

WITH CFG AS (
    SELECT EMAILNOTIFYQF
    FROM TGQCONFIG
    WHERE ROWNUM = 1
),
PARAM AS (
    SELECT NVL(
               (
                   SELECT INTEIRO
                   FROM TSIPAR
                   WHERE CHAVE = 'DIASVENCCQA'
                     AND CODUSU = STP_GET_CODUSULOGADO
               ),
               NVL(
                   (
                       SELECT INTEIRO
                       FROM TSIPAR
                       WHERE CHAVE = 'DIASVENCCQA'
                         AND CODUSU = 0
                   ),
                   0
               )
           ) AS DIASVENCCQA
    FROM DUAL
)

SELECT
    Q.IDQUALIF,
    C.IDCERTFORN AS IDREGISTRO,
    'CERTIFICADO' AS TIPO_VENCIMENTO,
    Q.CODPARC,
    PAR.NOMEPARC,
    PAR.EMAILQUESTIONARIO AS EMAIL_FORNECEDOR,
    CFG.EMAILNOTIFYQF AS EMAIL_NOTIFICACAO_EMPRESA,
    PARAM.DIASVENCCQA AS DIAS_PARAMETRO_AVISO,
    Q.IDQUEST,
    Q.QUALIFICACAO,
    C.DATAVALIDADE,
    TRUNC(C.DATAVALIDADE) - TRUNC(SYSDATE) AS DIAS_RESTANTES,
    NVL(C.DESCRCERT, 'Certificado ' || TO_CHAR(C.IDCERTFORN)) AS NOME_DOCUMENTO,
    CASE
        WHEN TRUNC(C.DATAVALIDADE) - TRUNC(SYSDATE) = PARAM.DIASVENCCQA
             AND PARAM.DIASVENCCQA > 0 THEN 'AVISO_VENCIMENTO'
        WHEN TRUNC(C.DATAVALIDADE) - TRUNC(SYSDATE) = 0 THEN 'VENCIMENTO_HOJE'
        WHEN TRUNC(C.DATAVALIDADE) - TRUNC(SYSDATE) &lt; 0 THEN 'VENCIDO'
        ELSE 'SEM_NOTIFICACAO'
    END AS TIPO_NOTIFICACAO,
    CASE
        WHEN TRUNC(C.DATAVALIDADE) - TRUNC(SYSDATE) = PARAM.DIASVENCCQA
             AND PARAM.DIASVENCCQA > 0 THEN 'S'
        ELSE 'N'
    END AS ENVIAR_NOTIFICACAO
FROM TGQCERTFORN C
INNER JOIN TGQQUALIFFORN Q ON Q.IDQUALIF = C.IDQUALIF
INNER JOIN TGFPAR PAR ON PAR.CODPARC = Q.CODPARC
CROSS JOIN CFG
CROSS JOIN PARAM
WHERE C.DATAVALIDADE IS NOT NULL

UNION ALL

SELECT
    Q.IDQUALIF,
    A.IDARQFORN AS IDREGISTRO,
    'ARQUIVO_QUESTIONARIO' AS TIPO_VENCIMENTO,
    Q.CODPARC,
    PAR.NOMEPARC,
    PAR.EMAILQUESTIONARIO AS EMAIL_FORNECEDOR,
    CFG.EMAILNOTIFYQF AS EMAIL_NOTIFICACAO_EMPRESA,
    PARAM.DIASVENCCQA AS DIAS_PARAMETRO_AVISO,
    Q.IDQUEST,
    Q.QUALIFICACAO,
    A.DATAVALIDADE,
    TRUNC(A.DATAVALIDADE) - TRUNC(SYSDATE) AS DIAS_RESTANTES,
    NVL(A.DESCRICAOARQ, 'Arquivo ' || TO_CHAR(A.IDARQFORN)) AS NOME_DOCUMENTO,
    CASE
        WHEN TRUNC(A.DATAVALIDADE) - TRUNC(SYSDATE) = PARAM.DIASVENCCQA
             AND PARAM.DIASVENCCQA > 0 THEN 'AVISO_VENCIMENTO'
        WHEN TRUNC(A.DATAVALIDADE) - TRUNC(SYSDATE) = 0 THEN 'VENCIMENTO_HOJE'
        WHEN TRUNC(A.DATAVALIDADE) - TRUNC(SYSDATE) &lt; 0 THEN 'VENCIDO'
        ELSE 'SEM_NOTIFICACAO'
    END AS TIPO_NOTIFICACAO,
    CASE
        WHEN TRUNC(A.DATAVALIDADE) - TRUNC(SYSDATE) = PARAM.DIASVENCCQA
             AND PARAM.DIASVENCCQA > 0 THEN 'S'
        ELSE 'N'
    END AS ENVIAR_NOTIFICACAO
FROM TGQARQQUESTFORN A
INNER JOIN TGQQUALIFFORN Q ON Q.IDQUALIF = A.IDQUALIF
INNER JOIN TGFPAR PAR ON PAR.CODPARC = Q.CODPARC
CROSS JOIN CFG
CROSS JOIN PARAM
WHERE A.DATAVALIDADE IS NOT NULL

        </oracle>
    </sql>
    <sql nomeObjeto="STATUS" tipoObjeto="COLUMN" nomeTabela="TGQCAUSARAIZ" executar="SE_NAO_EXISTIR" ordem="2">
        <oracle>

ALTER TABLE TGQCAUSARAIZ ADD STATUS VARCHAR2(2)

</oracle>
        <mssql>

ALTER TABLE TGQCAUSARAIZ ADD STATUS VARCHAR(2)

</mssql>
    </sql>
    <sql nomeObjeto="STATUS" tipoObjeto="COLUMN" nomeTabela="TGQACOESIMEDIATAS" executar="SE_NAO_EXISTIR" ordem="3">
        <oracle>

ALTER TABLE TGQACOESIMEDIATAS ADD STATUS VARCHAR2(2)

</oracle>
        <mssql>

ALTER TABLE TGQACOESIMEDIATAS ADD STATUS VARCHAR(2)

</mssql>
    </sql>
    <sql nomeObjeto="STATUS" tipoObjeto="COLUMN" nomeTabela="TGQABRANGENCIA" executar="SE_NAO_EXISTIR" ordem="4">
        <oracle>

ALTER TABLE TGQABRANGENCIA ADD STATUS VARCHAR2(2)

</oracle>
        <mssql>

ALTER TABLE TGQABRANGENCIA ADD STATUS VARCHAR(2)

</mssql>
    </sql>
    <sql nomeObjeto="STATUS" tipoObjeto="COLUMN" nomeTabela="TGQACOESCORRETIVAS" executar="SE_NAO_EXISTIR" ordem="5">
        <oracle>

ALTER TABLE TGQACOESCORRETIVAS ADD STATUS VARCHAR2(2)

</oracle>
        <mssql>

ALTER TABLE TGQACOESCORRETIVAS ADD STATUS VARCHAR(2)

</mssql>
    </sql>
    <sql nomeObjeto="STATUS" tipoObjeto="COLUMN" nomeTabela="TGQIMPLEMENTACAO" executar="SE_NAO_EXISTIR" ordem="6">
        <oracle>

ALTER TABLE TGQIMPLEMENTACAO ADD STATUS VARCHAR2(2)

</oracle>
        <mssql>

ALTER TABLE TGQIMPLEMENTACAO ADD STATUS VARCHAR(2)

</mssql>
    </sql>
    <sql nomeObjeto="CONCLUIDO" tipoObjeto="COLUMN" nomeTabela="TGQCAUSARAIZ" executar="SE_NAO_EXISTIR" ordem="7">
        <oracle>

ALTER TABLE TGQCAUSARAIZ ADD CONCLUIDO VARCHAR2(2)

</oracle>
        <mssql>

ALTER TABLE TGQCAUSARAIZ ADD CONCLUIDO VARCHAR(2)

</mssql>
    </sql>
    <sql nomeObjeto="CONCLUIDO" tipoObjeto="COLUMN" nomeTabela="TGQACOESIMEDIATAS" executar="SE_NAO_EXISTIR" ordem="8">
        <oracle>

ALTER TABLE TGQACOESIMEDIATAS ADD CONCLUIDO VARCHAR2(2)

</oracle>
        <mssql>

ALTER TABLE TGQACOESIMEDIATAS ADD CONCLUIDO VARCHAR(2)

</mssql>
    </sql>
    <sql nomeObjeto="CONCLUIDO" tipoObjeto="COLUMN" nomeTabela="TGQABRANGENCIA" executar="SE_NAO_EXISTIR" ordem="9">
        <oracle>

ALTER TABLE TGQABRANGENCIA ADD CONCLUIDO VARCHAR2(2)

</oracle>
        <mssql>

ALTER TABLE TGQABRANGENCIA ADD CONCLUIDO VARCHAR(2)

</mssql>
    </sql>
    <sql nomeObjeto="CONCLUIDO" tipoObjeto="COLUMN" nomeTabela="TGQACOESCORRETIVAS" executar="SE_NAO_EXISTIR" ordem="10">
        <oracle>

ALTER TABLE TGQACOESCORRETIVAS ADD CONCLUIDO VARCHAR2(2)

</oracle>
        <mssql>

ALTER TABLE TGQACOESCORRETIVAS ADD CONCLUIDO VARCHAR(2)

</mssql>
    </sql>
    <sql nomeObjeto="CONCLUIDO" tipoObjeto="COLUMN" nomeTabela="TGQIMPLEMENTACAO" executar="SE_NAO_EXISTIR" ordem="11">
        <oracle>

ALTER TABLE TGQIMPLEMENTACAO ADD CONCLUIDO VARCHAR2(2)

</oracle>
        <mssql>

ALTER TABLE TGQIMPLEMENTACAO ADD CONCLUIDO VARCHAR(2)

</mssql>
    </sql>
    <sql nomeObjeto="USUINCLUSAO" tipoObjeto="COLUMN" nomeTabela="TGQRNC" executar="SE_NAO_EXISTIR" ordem="12">
        <oracle>

ALTER TABLE TGQRNC ADD USUINCLUSAO NUMBER(38,0)

</oracle>
        <mssql>

ALTER TABLE TGQRNC ADD USUINCLUSAO INT

</mssql>
    </sql>
    <sql nomeObjeto="DATAINCLUSAO" tipoObjeto="COLUMN" nomeTabela="TGQRNC" executar="SE_NAO_EXISTIR" ordem="13">
        <oracle>

ALTER TABLE TGQRNC ADD DATAINCLUSAO DATE

</oracle>
        <mssql>

ALTER TABLE TGQRNC ADD DATAINCLUSAO DATETIME

</mssql>
    </sql>
    <sql nomeObjeto="TGQBASE" tipoObjeto="TABLE" nomeTabela="TGQBASE" executar="SE_NAO_EXISTIR" ordem="14">
        <oracle>

CREATE TABLE TGQBASE
(ID NUMBER(10,0),
DESCRICAO VARCHAR2(100),
USURESPONSAVEL NUMBER(38,0),
CONSTRAINT PK_TGQBASE PRIMARY KEY (ID))

</oracle>
        <mssql>

CREATE TABLE TGQBASE
(ID BIGINT,
DESCRICAO VARCHAR(100),
USURESPONSAVEL INT,
CONSTRAINT PK_TGQBASE PRIMARY KEY (ID))

</mssql>
    </sql>
    <sql nomeObjeto="FK_TGQBASE_USURESP" tipoObjeto="FOREIGN KEY" nomeTabela="TGQBASE" executar="SE_NAO_EXISTIR" ordem="15">
        <oracle>

ALTER TABLE TGQBASE ADD CONSTRAINT FK_TGQBASE_USURESP FOREIGN KEY (USURESPONSAVEL) REFERENCES TSIUSU(CODUSU)

</oracle>
        <mssql>

ALTER TABLE TGQBASE ADD CONSTRAINT FK_TGQBASE_USURESP FOREIGN KEY (USURESPONSAVEL) REFERENCES TSIUSU(CODUSU)

</mssql>
    </sql>
    <sql nomeObjeto="DATAPRAZO" tipoObjeto="COLUMN" nomeTabela="TGQIMPLEMENTACAO" executar="SE_NAO_EXISTIR" ordem="16">
        <oracle>

ALTER TABLE TGQIMPLEMENTACAO ADD DATAPRAZO DATE

</oracle>
        <mssql>

ALTER TABLE TGQIMPLEMENTACAO ADD DATAPRAZO DATETIME

</mssql>
    </sql>
    <sql nomeObjeto="TRG_STATUS_TGQCAUSARAIZ" tipoObjeto="TRIGGER" nomeTabela="TGQCAUSARAIZ" executar="SE_NAO_EXISTIR" ordem="17">
        <oracle>

CREATE OR REPLACE TRIGGER TRG_STATUS_TGQCAUSARAIZ
BEFORE INSERT OR UPDATE ON TGQCAUSARAIZ FOR EACH ROW
BEGIN
    IF INSERTING THEN
        :NEW.STATUS := 'E';
    ELSIF UPDATING THEN
        IF :NEW.CONCLUIDO = 'S' THEN
            :NEW.STATUS := 'C';
        END IF;
    END IF;
END;

</oracle>
        <mssql>

CREATE OR REPLACE TRIGGER TRG_STATUS_TGQCAUSARAIZ ON TGQCAUSARAIZ
AFTER INSERT, UPDATE AS
BEGIN
    IF EXISTS (SELECT 1 FROM DELETED)
    BEGIN
        UPDATE T SET STATUS = 'C'
        FROM TGQCAUSARAIZ T INNER JOIN INSERTED I ON T.IDCAUSARAIZ = I.IDCAUSARAIZ
        WHERE I.CONCLUIDO = 'S'
    END
    ELSE
    BEGIN
        UPDATE T SET STATUS = 'E'
        FROM TGQCAUSARAIZ T INNER JOIN INSERTED I ON T.IDCAUSARAIZ = I.IDCAUSARAIZ
    END
END;

</mssql>
    </sql>
    <sql nomeObjeto="TRG_STATUS_TGQACOESIMEDIATAS" tipoObjeto="TRIGGER" nomeTabela="TGQACOESIMEDIATAS" executar="SE_NAO_EXISTIR" ordem="18">
        <oracle>

CREATE OR REPLACE TRIGGER TRG_STATUS_TGQACOESIMEDIATAS
BEFORE INSERT OR UPDATE ON TGQACOESIMEDIATAS FOR EACH ROW
BEGIN
    IF INSERTING THEN
        :NEW.STATUS := 'E';
    ELSIF UPDATING THEN
        IF :NEW.CONCLUIDO = 'S' THEN
            :NEW.STATUS := 'C';
        END IF;
    END IF;
END;

</oracle>
        <mssql>

CREATE OR REPLACE TRIGGER TRG_STATUS_TGQACOESIMEDIATAS ON TGQACOESIMEDIATAS
AFTER INSERT, UPDATE AS
BEGIN
    IF EXISTS (SELECT 1 FROM DELETED)
    BEGIN
        UPDATE T SET STATUS = 'C'
        FROM TGQACOESIMEDIATAS T INNER JOIN INSERTED I ON T.IDACOES = I.IDACOES
        WHERE I.CONCLUIDO = 'S'
    END
    ELSE
    BEGIN
        UPDATE T SET STATUS = 'E'
        FROM TGQACOESIMEDIATAS T INNER JOIN INSERTED I ON T.IDACOES = I.IDACOES
    END
END;

</mssql>
    </sql>
    <sql nomeObjeto="TRG_STATUS_TGQABRANGENCIA" tipoObjeto="TRIGGER" nomeTabela="TGQABRANGENCIA" executar="SE_NAO_EXISTIR" ordem="19">
        <oracle>

CREATE OR REPLACE TRIGGER TRG_STATUS_TGQABRANGENCIA
BEFORE INSERT OR UPDATE ON TGQABRANGENCIA FOR EACH ROW
BEGIN
    IF INSERTING THEN
        :NEW.STATUS := 'E';
    ELSIF UPDATING THEN
        IF :NEW.CONCLUIDO = 'S' THEN
            :NEW.STATUS := 'C';
        END IF;
    END IF;
END;

</oracle>
        <mssql>

CREATE OR REPLACE TRIGGER TRG_STATUS_TGQABRANGENCIA ON TGQABRANGENCIA
AFTER INSERT, UPDATE AS
BEGIN
    IF EXISTS (SELECT 1 FROM DELETED)
    BEGIN
        UPDATE T SET STATUS = 'C'
        FROM TGQABRANGENCIA T INNER JOIN INSERTED I ON T.IDABRANGENCIA = I.IDABRANGENCIA
        WHERE I.CONCLUIDO = 'S'
    END
    ELSE
    BEGIN
        UPDATE T SET STATUS = 'E'
        FROM TGQABRANGENCIA T INNER JOIN INSERTED I ON T.IDABRANGENCIA = I.IDABRANGENCIA
    END
END;

</mssql>
    </sql>
    <sql nomeObjeto="TRG_STATUS_TGQACOESCORRETIVAS" tipoObjeto="TRIGGER" nomeTabela="TGQACOESCORRETIVAS" executar="SE_NAO_EXISTIR" ordem="20">
        <oracle>

CREATE OR REPLACE TRIGGER TRG_STATUS_TGQACOESCORRETIVAS
BEFORE INSERT OR UPDATE ON TGQACOESCORRETIVAS FOR EACH ROW
BEGIN
    IF INSERTING THEN
        :NEW.STATUS := 'E';
    ELSIF UPDATING THEN
        IF :NEW.CONCLUIDO = 'S' THEN
            :NEW.STATUS := 'C';
        END IF;
    END IF;
END;

</oracle>
        <mssql>

CREATE OR REPLACE TRIGGER TRG_STATUS_TGQACOESCORRETIVAS ON TGQACOESCORRETIVAS
AFTER INSERT, UPDATE AS
BEGIN
    IF EXISTS (SELECT 1 FROM DELETED)
    BEGIN
        UPDATE T SET STATUS = 'C'
        FROM TGQACOESCORRETIVAS T INNER JOIN INSERTED I ON T.IDACOES = I.IDACOES
        WHERE I.CONCLUIDO = 'S'
    END
    ELSE
    BEGIN
        UPDATE T SET STATUS = 'E'
        FROM TGQACOESCORRETIVAS T INNER JOIN INSERTED I ON T.IDACOES = I.IDACOES
    END
END;

</mssql>
    </sql>
    <sql nomeObjeto="TRG_STATUS_TGQIMPLEMENTACAO" tipoObjeto="TRIGGER" nomeTabela="TGQIMPLEMENTACAO" executar="SE_NAO_EXISTIR" ordem="21">
        <oracle>

CREATE OR REPLACE TRIGGER TRG_STATUS_TGQIMPLEMENTACAO
BEFORE INSERT OR UPDATE ON TGQIMPLEMENTACAO FOR EACH ROW
BEGIN
    IF INSERTING THEN
        :NEW.STATUS := 'E';
    ELSIF UPDATING THEN
        IF :NEW.CONCLUIDO = 'S' THEN
            :NEW.STATUS := 'C';
        END IF;
    END IF;
END;

</oracle>
        <mssql>

CREATE OR REPLACE TRIGGER TRG_STATUS_TGQIMPLEMENTACAO ON TGQIMPLEMENTACAO
AFTER INSERT, UPDATE AS
BEGIN
    IF EXISTS (SELECT 1 FROM DELETED)
    BEGIN
        UPDATE T SET STATUS = 'C'
        FROM TGQIMPLEMENTACAO T INNER JOIN INSERTED I ON T.IDIMP = I.IDIMP
        WHERE I.CONCLUIDO = 'S'
    END
    ELSE
    BEGIN
        UPDATE T SET STATUS = 'E'
        FROM TGQIMPLEMENTACAO T INNER JOIN INSERTED I ON T.IDIMP = I.IDIMP
    END
END;

</mssql>
    </sql>
</scripts>
